# Allows nginx to bind the floating VRRP VIP (172.21.129.200) before
# keepalived has actually brought that address up on this interface --
# needed on both LB nodes for HA SSH load balancing.
net.ipv4.ip_nonlocal_bind = 1
